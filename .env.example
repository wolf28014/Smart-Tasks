# ─── 数据库配置（可选） ─────────────────────────────────────
#
# ⚠️ 你不需要创建 .env 文件！项目默认使用 SQLite，开箱即用。
#
# 默认数据库路径：prisma/db/custom.db（在 prisma/schema.prisma 中配置）
#
# 只有在你想自定义数据库路径时，才需要：
#   1. 复制本文件为 .env：cp .env.example .env
#   2. 取消下面一行的注释并修改路径

# DATABASE_URL="file:./db/custom.db"

# 或者使用绝对路径：
# DATABASE_URL="file:/absolute/path/to/your/project/db/custom.db"
#
# 部署到 PostgreSQL 时改为：
# DATABASE_URL="postgresql://user:password@localhost:5432/todolist"
# 同时修改 prisma/schema.prisma 的 provider 为 "postgresql"
